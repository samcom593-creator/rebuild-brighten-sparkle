# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# APEX bot credentials — contains service_role references and bot bearer token
apex-bot-credentials.md
.env.local
.env.production
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.env.local
.vercel
supabase/.temp/
.env*.local

# wave-54b (2026-06-10): build-time emit by landingCssBuildPlugin() in
# vite.config.ts. Built fresh on every `vite build` / `vite dev` from
# tailwind.config.landing.cjs + src/index.css via scripts/build-landing-css.mjs.
# Not a source file — gitignored so `git status` stays clean post-build.
public/landing.css
