// ThemeToggle — DISABLED (2026-05-17).
// Light mode was causing readability problems sitewide. Locked to dark.
// Component returns null so existing call sites keep working.
export function ThemeToggle() {
  return null;
}
