export function formatClock(ms: number): string { const s = Math.max(0, Math.floor(ms / 1000)); return `${String(Math.floor(s / 60)).padStart(2, "0")}:${String(s % 60).padStart(2, "0")}`; }
export function formatDuration(ms: number): string { const s = Math.max(0, Math.floor(ms / 1000)); return `${Math.floor(s / 60)}:${String(s % 60).padStart(2, "0")}`; }
export type Snapshot = { title: string; data: { goal: string; agentGoal: string; winCondition: string; maxDurationSec: number; openingLine: string; difficulty: 1 | 2 | 3; estimatedMinutes: number; gradedSummary: string[]; skills: string[]; segment: string }; persona: { name: string; role: string; speakingStyle: string; voice: { voiceId?: string; label: string; pitchHint?: "low" | "mid" | "high" } }; objections: { id: string; key: string; required: boolean; title: string; data: { responsePrinciples: string[]; example?: string } }[]; rubric: { objectionStages: Record<string, number>; passRules: { requiredObjectionMin: number; overallMin: number }; deliveryBenchmarks?: { wordsPerMinute: [number, number]; fillersPerMinute: number; longestMonologueSec: number; talkRatioAgent: [number, number]; responseLatencyMs: [number, number] } } };
